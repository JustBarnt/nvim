;; extends

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "Helpers"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "Installables"))
